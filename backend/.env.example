# Copy this file to .env and edit as needed.
# .env is gitignored.

# SQLAlchemy database URL
# Development (default): local SQLite file
DATABASE_URL=sqlite:///./tarkov.db
# Production example (PostgreSQL):
# DATABASE_URL=postgresql://user:password@localhost/dbname

# Comma-separated list of allowed CORS origins (no spaces around commas).
# Development default: local file server on port 5500
CORS_ORIGINS=http://127.0.0.1:5500
# Production: set to your actual domain(s). When the frontend is served from the
# same origin as the API (e.g. via nginx proxy), CORS is not needed for the frontend
# itself, but you may still want to list the domain for safety.
# CORS_ORIGINS=https://example.com,https://www.example.com

# REQUIRED in production - server refuses to start without these.
# Use strong random values (e.g. openssl rand -hex 32).
IP_HASH_SECRET=changeme
ADMIN_API_KEY=changeme

# Comma-separated IPs of trusted reverse proxies whose X-Forwarded-For /
# X-Real-IP headers are honoured for client IP detection.
# Default: 127.0.0.1,::1 (loopback only, suitable for local nginx/Caddy)
# TRUSTED_PROXY_IPS=127.0.0.1,::1

# Set to 1 to re-enable /docs and /redoc (development only).
# ENABLE_API_DOCS=0

# Gitee personal access token for the background build-image migration worker.
# Create one at: https://gitee.com/profile/personal_access_tokens (scope: projects)
# GITEE_TOKEN=

# Set to 1 to test the migration worker without uploading anything to Gitee.
# No token required - logs what would be uploaded instead of doing it.
# GITEE_DRY_RUN=0

# Set to 1 to permanently disable the background migration worker.
# Use this once all existing builds have been migrated.
# DISABLE_BG_MIGRATE=0
